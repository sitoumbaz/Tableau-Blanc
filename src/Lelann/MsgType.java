package Lelann;

public enum MsgType {
	TOKEN, ROUTE, WHEREIS, HEREIS, READY, FORME;

	// route : envois message au voisin pour le connaitre
	// token : pas implémenté à voir
	// wheris : pour construire la table ex ( je suis le proc 1 je demande le
	// proc x "
	// hereis : réponse du msg précédent
	// ready quand chaqun a construit sa table de routage et qu'elle est
	// complete.

}
